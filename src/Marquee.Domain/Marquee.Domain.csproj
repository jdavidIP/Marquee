<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <TargetFramework>net9.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

  <ItemGroup>
    <!-- The password blocklist's data (issue #27) — see CommonPasswords.cs for provenance. -->
    <EmbeddedResource Include="Resources\common-passwords-top10000.txt" />
  </ItemGroup>

</Project>
